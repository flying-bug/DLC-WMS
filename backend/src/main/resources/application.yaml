spring:
  application:
    name: backend
  datasource:
    url: jdbc:mysql://localhost:3306/duylongcomputer?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  mail:
    host: smtp.gmail.com
    port: 587
    username: computerduylong@gmail.com
    password: sjvbpvpcyjcdqdun
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  jpa:
    hibernate:
      ddl-auto: update
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB

google:
  client-id: 889308816246-1sg2529hrhn6671gfcm2fae11eg9qque.apps.googleusercontent.com

cloudinary:
  url: ${CLOUDINARY_URL:}
  cloud-name: ${CLOUDINARY_CLOUD_NAME:dummy_cloud_name}
  api-key: ${CLOUDINARY_API_KEY:dummy_api_key}
  api-secret: ${CLOUDINARY_API_SECRET:dummy_api_secret}
  folder: ${CLOUDINARY_FOLDER:duy-long-computer}
  max-file-size: ${CLOUDINARY_MAX_FILE_SIZE:5242880}

ai:
  provider: ${AI_PROVIDER:openai}
  openai:
    enabled: ${OPENAI_ENABLED:false}
    api-key: ${OPENAI_API_KEY:}
    model: ${OPENAI_MODEL:gpt-4o-mini}
    base-url: ${OPENAI_BASE_URL:https://api.openai.com/v1}
  gemini:
    enabled: ${GEMINI_ENABLED:false}
    api-key: ${GEMINI_API_KEY:}
    model: ${GEMINI_MODEL:gemini-2.0-flash}
    base-url: ${GEMINI_BASE_URL:https://generativelanguage.googleapis.com/v1beta}
